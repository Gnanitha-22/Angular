# Folder Structure

 - `src/app/core` — services, interceptors, guards, resolvers, config
 - `src/app/features` — feature pages and standalone components (auth, users, projects, tasks, reports)
 - `src/app/shared` — shared components, utilities, pipes
 - `src/assets` — static assets
 - `db.json` — mock JSON Server backend
